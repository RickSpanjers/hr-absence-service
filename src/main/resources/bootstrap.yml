spring:
  profiles:
    active: dev

---

spring:
  profiles: dev
  application:
    name: absence-service
  cloud:
    config:
      uri:
      username:
      password:

---

spring:
  profiles: prod
  application:
    name: absence-service
  cloud:
    config:
      uri:
      username:
      password: